This project contains 2 parts.

1. Data API built with HTTP GET and POST command, that query a specific records by HID and post new record to the CSV file.

2. A Bayesian model built with Python that predicts which record of posting actually is a commerical space rental advertisement, aka has_space column is 1.

    